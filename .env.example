# =====================================================
# Certi-Hub 환경 변수 설정
# 이 파일을 .env로 복사해서 사용하세요
# cp .env.example .env
# =====================================================

# === PostgreSQL ===
POSTGRES_DB=certihub
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# === Backend (FastAPI) ===
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/certihub
DATABASE_URL_SYNC=postgresql+psycopg2://postgres:postgres@db:5432/certihub
FRONTEND_URL=http://localhost:3000
DEBUG=true

# === Frontend (Next.js) ===
NEXT_PUBLIC_API_URL=http://localhost:8000

# === GitHub Actions Secrets (CI/CD에서 사용) ===
# DATABASE_URL=postgresql+psycopg2://<user>:<password>@<host>:5432/certihub
