# =====================================================
# Certi-Hub 환경변수 설정
# 이 파일을 복사하여 .env 파일을 생성하세요:
#   cp .env.example .env
# =====================================================

# ===== 데이터베이스 (필수) =====
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here  # 반드시 변경하세요!

# ===== 앱 설정 =====
DEBUG=false                    # 개발: true, 프로덕션: false
LOG_LEVEL=INFO                 # DEBUG, INFO, WARNING, ERROR

# ===== 도메인 / URL =====
FRONTEND_URL=https://yourdomain.kr          # 프론트엔드 접속 URL
NEXT_PUBLIC_API_URL=https://yourdomain.kr   # 클라이언트에서 API 호출 URL

# ===== CORS 허용 도메인 (쉼표 구분) =====
# FRONTEND_URL은 자동 포함, 추가 도메인이 있을 때만 설정
ALLOWED_ORIGINS=https://www.yourdomain.kr

# ===== 포트 설정 =====
APP_PORT=80                    # Nginx HTTP 포트
SSL_PORT=443                   # Nginx HTTPS 포트
# DB_PORT=5432                 # 프로덕션에서는 외부 노출하지 않음

# ===== SSL 설정 =====
NGINX_CONF=nginx.ssl.conf      # HTTP만: nginx.conf / HTTPS: nginx.ssl.conf
SSL_CERT_PATH=/etc/letsencrypt  # Let's Encrypt 인증서 경로

# ===== 크롤링 API 키 =====
DATA_GO_KR_API_KEY=            # 공공데이터포털 API 키 (선택)

# ===== Supabase (선택, 클라우드 DB 사용 시) =====
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_ANON_KEY=eyJ...
