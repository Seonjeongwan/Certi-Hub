# 🏗️ 시스템 아키텍처

> Certi-Hub의 전체 시스템 구조, 서비스 간 통신, 데이터 흐름을 설명합니다.

---

## 📐 전체 아키텍처 다이어그램

```
                        ┌─────────────────────────────────────┐
                        │            사용자 (브라우저)           │
                        └──────────────┬──────────────────────┘
                                       │ HTTP/HTTPS
                                       ▼
                        ┌──────────────────────────────────────┐
                        │        Nginx (리버스 프록시)           │
                        │   • SSL 종료 (Let's Encrypt)          │
                        │   • Rate Limiting (API: 30r/s)        │
                        │   • Gzip 압축                         │
                        │   • 정적 파일 캐싱                     │
                        │   • 보안 헤더 (HSTS, CSP, XSS)        │
                        └───────┬────────────────┬─────────────┘
                     /api/*     │                │    /*
                                ▼                ▼
              ┌─────────────────────┐  ┌─────────────────────────┐
              │  FastAPI Backend     │  │  Next.js Frontend        │
              │  (Python 3.12)      │  │  (React 18 + TypeScript) │
              │                     │  │                           │
              │  • REST API         │  │  • SSR / CSR 하이브리드    │
              │  • 비동기 (asyncpg)  │◄─┤  • Route Handler (프록시) │
              │  • CORS 미들웨어     │  │  • Fallback seed-data    │
              │  • 요청 로깅         │  │  • FullCalendar 연동     │
              │  • APScheduler      │  │  • Tailwind CSS          │
              │  • 크롤러 엔진       │  │  • Axios + 재시도        │
              └─────────┬───────────┘  └───────────────────────────┘
                        │
                        ▼
              ┌─────────────────────┐
              │  PostgreSQL 16      │
              │  (Alpine)           │
              │                     │
              │  • certifications   │
              │  • exam_schedules   │
              │  • crawl_logs       │
              │  • UUID-OSSP        │
              │  • ENUM Types       │
              │  • 자동 트리거       │
              └─────────────────────┘
```

---

## 🔄 서비스 간 통신 구조

### Docker Compose 네트워크

모든 서비스는 Docker Compose의 기본 브리지 네트워크 내에서 **컨테이너 이름**으로 통신합니다.

```
┌─────────────────── Docker Network (certihub_default) ───────────────────┐
│                                                                         │
│  nginx:80/443  ──────►  frontend:3000                                   │
│       │                      │                                          │
│       └──────────────►  backend:8000  ──────►  db:5432                  │
│                              │                                          │
│                        [crawler_cache]     [postgres_data]               │
│                        [frontend/lib]     (볼륨 마운트)                   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 포트 매핑 (개발 환경)

| 서비스 | 컨테이너 포트 | 호스트 포트 | 설명 |
|--------|-------------|------------|------|
| `db` | 5432 | 5432 | PostgreSQL |
| `backend` | 8000 | 8000 | FastAPI |
| `frontend` | 3000 | 3000 | Next.js |

### 포트 매핑 (프로덕션)

| 서비스 | 컨테이너 포트 | 호스트 포트 | 설명 |
|--------|-------------|------------|------|
| `nginx` | 80, 443 | 80, 443 | 유일한 외부 노출 포트 |
| `db` | 5432 | - | 내부 전용 (노출 안 함) |
| `backend` | 8000 | - | Nginx 경유만 허용 |
| `frontend` | 3000 | - | Nginx 경유만 허용 |

---

## 📡 요청 처리 흐름

### 1. 클라이언트 → 프론트엔드 (일반 페이지)

```
브라우저 → Nginx (/) → Next.js (SSR/CSR) → 브라우저에 HTML 렌더링
```

### 2. 클라이언트 → API 요청 (자격증/일정 데이터)

```
브라우저 (Axios)
  │
  │ 1) 상대 경로: /api/certifications
  ▼
Next.js Route Handler (서버사이드)
  │
  │ 2) 내부 통신: http://backend:8000/api/certifications
  ▼
FastAPI Backend
  │
  │ 3) 비동기 DB 쿼리
  ▼
PostgreSQL
  │
  │ 4) 결과 반환
  ▼
Next.js Route Handler
  │
  │ 5) JSON 응답 (5분 캐시 revalidate)
  ▼
브라우저 (데이터 렌더링)
```

### 3. API 장애 시 Fallback 흐름

```
브라우저 (Axios)
  │
  │ 1) /api/certifications 요청
  ▼
Next.js Route Handler
  │
  │ 2) backend:8000 연결 실패 (타임아웃/에러)
  ▼
Fallback: seed-data.ts 반환 (정적 데이터)
  │
  │ 3) 캐시된 자격증 데이터로 응답
  ▼
브라우저 (경고 배너 + 캐시 데이터 표시)
```

### 4. 크롤링 데이터 흐름

```
APScheduler (매일 03:00 KST)
  │
  │ 1) run_crawl_job("all") 실행
  ▼
6개 크롤러 순차 실행
  │
  │ 2) 3단계 Fallback: API → 크롤링 → 캐시
  ▼
┌──────────────────────────────────────────┐
│ 크롤러별 데이터 수집                       │
│  • Q-Net:        공공데이터포털 API         │
│  • KData:        dataq.or.kr API/크롤링    │
│  • Cloud:        AWS/GCP/Azure 벤더 API    │
│  • Finance:      KOFIA/KBI AJAX API        │
│  • IT Domestic:  ICQA/IHD 기관 웹 크롤링   │
│  • International:ISC2/Cisco/Oracle 벤더 API │
└──────────────┬───────────────────────────┘
               │
               │ 3) DB 업서트 (Conflict Resolution)
               ▼
         PostgreSQL
         (exam_schedules 테이블)
               │
               │ 4) CrawlLog 기록
               ▼
         crawl_logs 테이블
               │
               │ 5) seed-events.ts 자동 동기화
               ▼
   frontend/lib/seed-events.ts (Fallback 데이터 갱신)
```

---

## 🗂️ 계층 구조 (레이어 아키텍처)

### Backend 레이어

```
┌─────────────────────────────────────────────────────┐
│                  Presentation Layer                   │
│   main.py (FastAPI App)                              │
│   middleware.py (로깅, 에러 핸들링)                     │
│   routers/ (certifications, schedules, crawl)        │
├─────────────────────────────────────────────────────┤
│                  Business Logic Layer                 │
│   services/cert_service.py (관련 자격증, Upsert)       │
│   services/scheduler.py (APScheduler, 크롤 잡)        │
│   services/seed_sync.py (DB→seed-events.ts 동기화)   │
├─────────────────────────────────────────────────────┤
│                  Data Crawler Layer                   │
│   crawlers/base.py (3단계 Fallback 베이스 클래스)      │
│   crawlers/qnet_scraper.py (Q-Net)                   │
│   crawlers/kdata_scraper.py (KData)                  │
│   crawlers/cloud_scraper.py (Cloud Vendor)           │
│   crawlers/finance_scraper.py (금융)                  │
│   crawlers/it_domestic_scraper.py (국내 IT)           │
│   crawlers/intl_cert_scraper.py (국제 CBT)           │
│   crawlers/run_crawlers.py (오케스트레이터)             │
├─────────────────────────────────────────────────────┤
│                  Data Access Layer                    │
│   database.py (SQLAlchemy Async Engine, Session)     │
│   models.py (ORM 모델: Certification, ExamSchedule,  │
│              CrawlLog)                               │
│   schemas.py (Pydantic 스키마: 요청/응답 직렬화)        │
├─────────────────────────────────────────────────────┤
│                  Infrastructure Layer                 │
│   config.py (환경변수 관리, pydantic-settings)         │
│   logging_config.py (구조적 로깅 설정)                 │
└─────────────────────────────────────────────────────┘
```

### Frontend 레이어

```
┌─────────────────────────────────────────────────────┐
│                  Page / Layout Layer                  │
│   app/layout.tsx (RootLayout, SEO, JSON-LD)          │
│   app/page.tsx (메인 페이지, 상태 관리 허브)            │
├─────────────────────────────────────────────────────┤
│                  Component Layer                      │
│   components/Header.tsx (네비게이션, 검색)              │
│   components/Hero.tsx (히어로, 자동완성 검색)           │
│   components/StickyTagBar.tsx (카테고리 필터, Sticky)  │
│   components/PopularGrid.tsx (인기 자격증 그리드)       │
│   components/RoadmapSection.tsx (로드맵 테이블/카드)    │
│   components/CertList.tsx (자격증 전체 목록)            │
│   components/CertCard.tsx (개별 자격증 카드)            │
│   components/CertModal.tsx (자격증 상세 모달)           │
│   components/CalendarSection.tsx (FullCalendar)       │
│   components/ErrorBoundary.tsx (에러 경계)             │
│   components/LoadingSkeleton.tsx (로딩 스켈레톤)        │
│   components/ScrollToTop.tsx (최상단 이동 버튼)         │
│   components/Footer.tsx (푸터)                        │
├─────────────────────────────────────────────────────┤
│                  API Proxy Layer                      │
│   app/api/certifications/route.ts (자격증 프록시)      │
│   app/api/schedules/route.ts (일정 프록시)             │
├─────────────────────────────────────────────────────┤
│                  Data / Utility Layer                 │
│   lib/api.ts (Axios 클라이언트, 재시도 인터셉터)        │
│   lib/types.ts (TypeScript 인터페이스)                 │
│   lib/constants.ts (태그 스타일, 레벨 라벨)             │
│   lib/seed-data.ts (자격증 Fallback 정적 데이터)       │
│   lib/seed-events.ts (캘린더 Fallback, 자동 생성)      │
│   lib/uuid.ts (UUID 유틸리티)                         │
└─────────────────────────────────────────────────────┘
```

---

## 🔐 보안 아키텍처

### 네트워크 보안

```
인터넷
  │
  │  ✅ Nginx: HTTPS (TLS 1.2/1.3), HSTS
  │  ✅ Rate Limiting: API 30r/s, General 60r/s
  │  ✅ 보안 헤더: X-Frame-Options, X-XSS-Protection, CSP
  ▼
Nginx ─── Docker Network (내부) ──── Backend / Frontend / DB
                                        │
                                        │  ✅ DB 포트 외부 미노출 (프로덕션)
                                        │  ✅ Non-root 컨테이너 사용자
                                        │  ✅ CORS 도메인 제한 (프로덕션)
                                        │  ✅ 입력 검증 (Pydantic + max_length)
                                        │  ✅ SQL Injection 방지 (SQLAlchemy ORM)
                                        │  ✅ 에러 상세 정보 비노출 (DEBUG=false)
                                        ▼
```

### 인증 / 권한 모델

현재 버전은 **공개 읽기 전용 서비스**입니다:
- 모든 GET API는 인증 없이 접근 가능
- POST/PATCH/DELETE API는 내부 관리용 (Nginx에서 접근 제한 가능)
- 향후 Supabase Auth 또는 JWT 인증 추가 가능 (설정 준비됨)

---

## ⚙️ 환경 분리

### 개발 환경 (`docker-compose.yml`)

```
• 소스 코드 바인드 마운트 (Hot Reload)
• uvicorn --reload (백엔드 자동 리로드)
• npm run dev (프론트엔드 HMR)
• DEBUG=true (상세 로깅, DB 에러 노출)
• DB 포트 외부 노출 (로컬 접근용)
• Nginx 없이 직접 접근 가능
```

### 프로덕션 환경 (`docker-compose.prod.yml`)

```
• 멀티스테이지 빌드 (최소 이미지)
• Gunicorn + Uvicorn Workers (멀티프로세스)
• Next.js standalone 출력 (최적화)
• DEBUG=false (에러 상세 정보 숨김)
• Nginx 리버스 프록시 필수
• SSL/HTTPS 적용
• 로그 로테이션 (10MB × 3)
• 헬스체크 활성화
• Non-root 사용자
• DB 포트 미노출
```

---

## 📊 확장성 고려사항

### 수평 확장 가능 포인트

| 구성 요소 | 확장 방법 |
|-----------|----------|
| **Backend** | Uvicorn Workers 수 조정 (`--workers 4`), 컨테이너 복제 + 로드밸런서 |
| **Frontend** | Next.js standalone은 stateless → 컨테이너 복제 가능 |
| **Database** | Read Replica 추가, Connection Pooling (PgBouncer) |
| **크롤러** | 개별 크롤러를 독립 Worker로 분리 가능 |
| **캐시** | Redis 도입으로 API 응답 캐싱 가능 |

### 현재 성능 최적화

- **DB Connection Pool**: pool_size=10, max_overflow=5, pool_pre_ping
- **비동기 I/O**: asyncpg + SQLAlchemy Async (논블로킹 DB 쿼리)
- **프론트엔드 캐싱**: Route Handler 5분 revalidate, 정적 파일 1년 캐시
- **Gzip 압축**: Nginx에서 텍스트 기반 응답 압축
- **API 재시도**: 지수 백오프 (1s → 2s → 4s), 최대 3회
- **메모이제이션**: React useMemo/useCallback, Python lru_cache

---

## 🔗 관련 문서

| 문서 | 설명 |
|------|------|
| [서비스 개요](./service-overview.md) | 서비스 소개, 주요 기능 |
| [기술 스택](./tech-stack.md) | 사용 기술 상세 |
| [API 명세](./api-reference.md) | REST API 엔드포인트 |
| [데이터베이스 스키마](./database-schema.md) | ERD, 테이블 구조 |
| [크롤러 시스템](./crawler-system.md) | 크롤러 운영 가이드 |
| [프론트엔드 가이드](./frontend-guide.md) | 컴포넌트 구조 |
| [배포 가이드](./deployment-guide.md) | Docker / CI/CD 배포 |
